import { createStore } from 'vuex'
import allContact from "./modules/allContact";

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    allContact
  }
})
